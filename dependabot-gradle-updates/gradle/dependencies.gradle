ext {
  libraries = [:]
}

libraries.junit4 = 'junit:junit:4.12'
libraries.appcompat = "androidx.appcompat:appcompat:1.3.0"

def kotlinVersion = '1.5.10'
def coroutinesVersion = '1.6.3'
libraries.kotlin = [
    version: kotlinVersion,
    gradlePlugin: "org.jetbrains.kotlin:kotlin-gradle-plugin:${kotlinVersion}"

    stdlib: "org.jetbrains.kotlin:kotlin-stdlib:${kotlinVersion}",
    coroutines: "org.jetbrains.kotlinx:kotlinx-coroutines-core:${coroutinesVersion}",
    coroutines: "org.jetbrains.kotlinx:kotlinx-coroutines-core:${coroutinesVersion}",
]
